Task: Create endpoint GET /v1/product/{id}

Goal:
Implement the endpoint following the existing project architecture and conventions.

---

1. Route
- File: routes/api.php
- Method: GET
- URI: /v1/product/{id}
- Controller: ProductController@getProductById

---

2. Controller

Method: getProductById

Responsibilities:
- Receive request (validated via FormRequest)
- Call service layer
- Return JSON response using HATEOAS pattern

Response structure:
return response()->json([
    'status' => true,
    'message' => '',
    'errors' => [],
    'data' => $data,
    '_links' => [
        'self' => url("/v1/product/{$id}"),
        'delete' => url("/v1/product/{$id}"),
        'update' => url("/v1/product/{$id}"),
        'create' => url("/v1/product"),
    ]
]);

---

3. Validation (FormRequest)

Class:
App\Http\Requests\GetProductByIdRequest

Rules:
- id: required | integer | min:1

Override validationData():
- Merge route parameters into request data

Override failedValidation():
- Throw App\Exceptions\InvalidParametersException

---

4. Service Layer

Class:
App\Services\ProductService

Method:
getProductById($id)

Responsibilities:
- Call App\Services\Product\GetProductById::execute($id)

Dependency:
- Inject GetProductById via constructor

---

5. Use Case

Class:
App\Services\Product\GetProductById

Responsibilities:
- Retrieve product via repository
- Apply business rules
- Return product

Flow:
1. Call repository->findById($id)
2. If not found:
   - Throw ResourceNotFoundException
3. Validate rules:
   - ValidateProductNotDeleted
4. Return product

---

6. Repository

Interface:
App\Repository\Contracts\ProductInterface

Implementation:
App\Repository\ProductRepository

Details:
- Use Eloquent (Product model)
- Ensure binding in AppServiceProvider
- Use migration:
  database/migrations/2026_02_11_091120_create_products_table

---

7. Rules

Class:
App\Services\Product\Rules\ValidateProductNotDeleted

Method:
validate($product)

Rule:
- If $product->deleted_at !== null:
  - Throw BusinessRuleException

Constraint:
- Do NOT access database
- Use only provided product object

---

8. Reference

Use existing endpoint as base:
DELETE /v1/product/type/{id}

---

9. Expected Output

- Full implementation
- All created/modified files
- Proper namespaces and imports
- Production-ready code (no pseudo-code)
